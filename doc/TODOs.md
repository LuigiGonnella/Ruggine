# fixare notifiche
# notifiche
# numerini per aggiornare notifica


Quando siamo in chat (privata/gruppo) c'è un polling attivo che prende in continuazione i messaggi dal db ogni 100ms, quando usciamo dalla chat il polling si interrompe.